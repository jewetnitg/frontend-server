import _ from 'lodash';

describe(`Server`, () => {

  function resetMocks() {
  }

  beforeEach(done => {
    resetMocks();
    done();
  });

  it(`should be a function`, done => {
    //expect(Server).to.be.a('function');
    done();
  });

});